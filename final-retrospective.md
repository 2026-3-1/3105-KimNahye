# 최종 회고 — Solving Meal 프로젝트 전체 회고

## 프로젝트 개요

**Solving Meal**은 자취생을 위한 요리 교육 플랫폼으로, 3단계(P1 → P2 → P3)에 걸쳐 점진적으로 기능을 확장한 풀스택 프로젝트입니다. NestJS 백엔드와 React 프론트엔드를 기반으로, 기초 CRUD부터 JWT 인증, 역할 기반 접근 제어, 결제 시스템, 성능 최적화까지 실무 수준의 기능을 구현했습니다.

---

## 학습 내용

### NestJS 실전 패턴
- **모듈 아키텍처**: 도메인 단위로 모듈을 분리하고 의존성을 명확히 선언하는 설계 방식을 체득했습니다. 각 모듈이 독립적으로 테스트 가능한 구조의 중요성을 실감했습니다.
- **Guard / Interceptor / Decorator**: `JwtAuthGuard`, `RolesGuard`, `CacheInterceptor`, `@CurrentUser()` 커스텀 데코레이터 등 NestJS의 선언적 패턴을 적극 활용했습니다.
- **의존성 주입(DI)**: 서비스 간 순환 의존을 피하는 설계와 `forwardRef()` 활용 방법을 배웠습니다.

### JWT RTR(Refresh Token Rotation) 전략
- Access Token(15분)과 Refresh Token(7일)의 역할 분리
- Refresh Token을 Redis에 저장해 단일 사용(1회용) 처리하는 RTR 전략 구현
- 토큰 탈취 시나리오에서 RTR이 어떻게 보안을 강화하는지 이해

### 결제 연동 (Toss Payments)
- PG사 결제 플로우 이해: 클라이언트 결제 요청 → 서버 검증 → Webhook 수신
- 결제 상태 관리(`PENDING`, `PAID`, `FAILED`, `CANCELLED`)와 트랜잭션 처리
- 외부 API 의존성의 취약점 경험 (INC-2026-001) → Retry + Exponential Backoff의 필요성 체감

### Docker 컨테이너화
- `docker-compose.yml`로 백엔드, 프론트엔드, PostgreSQL, Redis를 단일 명령으로 실행하는 환경 구성
- 환경별 설정 분리 (`.env.development`, `.env.production`)
- 헬스체크, 볼륨 마운트, 네트워크 격리 개념 적용

### 성능 최적화
- Redis 캐싱으로 API 응답 시간 최대 94% 개선
- DB 인덱스 설계의 중요성: 올바른 인덱스 하나가 쿼리 타임을 320ms → 12ms로 단축
- 프론트엔드 코드 스플리팅으로 초기 번들 크기 73% 감소

### 구조화 로그 및 모니터링
- Winston을 이용한 JSON 형식 로그, 로그 레벨 분리, 파일 로테이션
- 요청 ID(`requestId`) 기반 로그 추적으로 특정 요청의 전체 흐름 파악

---

## 잘한 점

### 명확한 모듈 분리
프로젝트 시작부터 도메인 단위로 모듈을 분리(courses, enrollments, payments, auth, users 등)해 코드 응집도를 높이고 관심사를 격리했습니다. 후반 P3 기능 추가 시에도 기존 모듈을 건드리지 않고 새 모듈을 추가하는 방식으로 확장이 자연스럽게 이루어졌습니다.

### 보안 설계
- **SQL Injection 방어**: TypeORM의 파라미터 바인딩으로 Raw Query를 최소화
- **XSS 방어**: `helmet` 미들웨어로 보안 헤더 자동 설정, 사용자 입력 검증에 `class-validator` 적용
- **인증 보안**: RTR 전략으로 Refresh Token 재사용 공격 방어, bcrypt 12 라운드 해싱
- **RBAC**: 역할에 따른 API 접근 제어로 인가 로직을 중앙화

### 구조화 로그 (Structured Logging)
JSON 형식의 로그로 로그 파싱 및 검색이 용이하고, `requestId`를 모든 로그에 포함해 분산 추적이 가능한 구조를 갖추었습니다. 장애 발생 시 신속한 원인 파악에 실제로 도움이 되었습니다.

### 점진적 개발 방식
P1 → P2 → P3 순서로 명확한 마일스톤을 설정하고, 각 단계에서 완성된 결과물을 만든 뒤 다음 단계로 진행한 것이 전체 완성도를 높이는 데 효과적이었습니다.

---

## 개선점

### 테스트 커버리지 부족
프로젝트 전체에 걸쳐 유닛 테스트와 통합 테스트가 충분히 작성되지 않았습니다. 리팩토링 시 회귀 여부를 수동으로 확인해야 하는 상황이 반복되었습니다.

- **목표**: 핵심 Service 로직 유닛 테스트 커버리지 80% 이상
- **해결책**: Jest + `@nestjs/testing` + 테스트 전용 PostgreSQL 컨테이너 (`testcontainers`) 도입

### 성능 모니터링 체계화 필요
Redis 캐싱과 DB 인덱스 적용 전후를 Apache Bench로 측정했지만, 지속적인 모니터링 체계가 없었습니다. 특정 시점의 스냅샷만으로는 트렌드를 파악하기 어렵습니다.

- **해결책**: Prometheus + Grafana 연동, NestJS `@nestjs/terminus` 헬스체크 고도화

### 에러 처리 일관성
P1에서 지적된 에러 응답 포맷 불일치가 P2까지 일부 남아 있었습니다. P3에서 전역 `HttpExceptionFilter`를 적용했지만, 처음부터 표준화했다면 프론트엔드 에러 처리 코드를 훨씬 단순하게 작성할 수 있었을 것입니다.

### CI/CD 파이프라인 미구성
GitHub Actions를 통한 자동 빌드/테스트 파이프라인이 없어, 모든 검증을 로컬에서 수동으로 진행했습니다. PR 시 자동 테스트 실행이 없어 코드 품질 관리에 한계가 있었습니다.

---

## 다음 단계

### 단기 (1~2개월)
- **E2E 테스트 추가**: Cypress 또는 Playwright로 핵심 사용자 플로우(회원가입 → 로그인 → 강좌 수강 → 결제) 자동화
- **CI/CD 파이프라인 구축**: GitHub Actions로 PR마다 빌드/테스트/Lint 자동 실행
- **유닛 테스트 보완**: 결제 서비스, 인증 서비스 등 핵심 로직 유닛 테스트 작성

### 중기 (3~6개월)
- **부하 테스트**: k6 또는 Locust를 이용한 실제 트래픽 시뮬레이션 및 병목 지점 파악
- **모니터링 대시보드**: Prometheus + Grafana 연동, API 레이턴시 / 에러율 / DB 커넥션 수 실시간 시각화
- **외부 API 안정성 강화**: Circuit Breaker 패턴 도입 (INC-2026-001 재발 방지)

### 장기 (6개월 이상)
- **Kubernetes 도입 검토**: 트래픽 증가 대비 자동 스케일링, 롤링 업데이트, 셀프힐링 환경 구성
- **소셜 로그인 추가**: Kakao / Google OAuth 2.0 연동으로 회원가입 허들 낮추기
- **영상 강의 지원**: AWS S3 + CloudFront를 이용한 동영상 업로드 및 스트리밍

---

## 마무리

Solving Meal 프로젝트를 통해 NestJS 풀스택 개발의 전체 사이클 — 설계, 구현, 보안, 성능 최적화, 운영 준비 — 을 직접 경험했습니다. 특히 실제 장애를 경험하고 사후 보고서를 작성하며 외부 의존성 관리의 중요성을 체감한 것이 가장 큰 수확이었습니다. 테스트와 모니터링에 더 일찍 투자했다면 더 높은 품질의 결과물을 만들 수 있었을 것이라는 교훈을 안고 다음 프로젝트에 임하겠습니다.

---

*작성일: 2026-06-02*
