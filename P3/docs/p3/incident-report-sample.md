# 사고 보고서 샘플 — Solving Meal

---

## 사고 요약

| 항목 | 내용 |
|---|---|
| **사고 ID** | INC-2026-001 |
| **날짜/시간** | 2026-06-15 14:32 KST |
| **심각도** | P2 (서비스 부분 장애) |
| **영향 범위** | 결제 기능 전체 (약 23분) |
| **담당자** | Knhye |
| **상태** | 해결 완료 |

---

## 타임라인

| 시각 | 이벤트 |
|---|---|
| 14:32 | 사용자 결제 실패 제보 접수 |
| 14:35 | 에러 로그 확인 — `TOSS_SECRET_KEY` 환경변수 누락 확인 |
| 14:40 | `.env` 파일에서 키 누락 원인 파악 (배포 시 시크릿 미동기화) |
| 14:50 | GitHub Actions Secrets에 `TOSS_SECRET_KEY` 재등록 |
| 14:55 | 재배포 완료 |
| 14:55 | 결제 기능 정상 복구 확인 |

---

## 근본 원인

배포 파이프라인에서 GitHub Secrets → 서버 `.env` 동기화 과정에 `TOSS_SECRET_KEY`가 누락됨.
기존 서버의 `.env`를 직접 수정했으나, 새 배포 시 덮어써지면서 키가 사라짐.

---

## 영향

- 결제 승인 API (`POST /api/v1/payments/confirm`) 500 에러
- 영향받은 사용자: 추정 3~5명 (결제 시도 로그 기준)
- 데이터 손실: 없음 (결제 미완료 상태로 보관됨)

---

## 조치 사항

### 즉시 조치
- GitHub Secrets에 `TOSS_SECRET_KEY` 재등록 후 재배포

### 재발 방지
- [ ] 배포 스크립트에 필수 환경변수 존재 여부 검증 단계 추가
- [ ] `.env.example`에 모든 필수 키 문서화 및 최신화
- [ ] 배포 후 헬스체크 + 결제 테스트 API 스모크 테스트 추가

---

## 교훈

환경변수는 서버에 직접 저장하지 않고 **CI/CD 시크릿으로만 관리**해야 함.
배포 절차서(Runbook)에 배포 후 스모크 테스트 항목을 추가함.

---

> 이 문서는 사고 보고서 작성 가이드용 샘플입니다.
> 실제 사고 발생 시 이 템플릿을 복사해서 작성하세요.
